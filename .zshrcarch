ZSH=$HOME/.oh-my-zsh
ZSH_THEME=agnoster

source ~/.zsh/git

zstyle ':completion:*' matcher-list 'm:{a-z}={A-Z}'
zstyle ':completion:*' insert-tab pending

CASE_SENSITIVE="true"
DISABLE_AUTO_UPDATE="true"

export UPDATE_ZSH_DAYS=14

plugins=(git)

source $ZSH/oh-my-zsh.sh

export PATH=$PATH:/usr/lib/qt4/bin:/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin:/usr/bin/core_perl

alias :q='exit'
alias :e='vim'
alias :te='vim -u NONE -N'
alias cat='cat @1 | less'

# Pacman

alias pacs='sudo pacman -S'
alias pacu='sudo pacman -U'
alias pacsyy='sudo pacman -Syy'
alias pacsyyu='sudo pacman -Syyu'
alias pacq='sudo pacman -Q'
alias pacr='sudo pacman -R'
alias pacupkg='sudo pacman -U *.tar.xz'
